# Core Data Processing (Upgraded for Python 3.14 compatibility)
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=14.0.0

# Spark (PySpark 3.4 cannot build on Python 3.14; bumped to modern)
pyspark>=3.5.0

# Database Connectors (Fixed SQLAlchemy conflict with Airflow & bumped psycopg2)
psycopg2-binary>=2.9.9
sqlalchemy>=1.4.28,<2.0

# Kafka
kafka-python==2.0.2

# MinIO / S3
boto3==1.34.0
minio==7.2.0

# API Framework
fastapi==0.104.0
uvicorn==0.24.0
pydantic>=2.4.0
requests==2.31.0
httpx==0.25.0

# Visualization
matplotlib>=3.8.0
seaborn==0.12.2
plotly==5.17.0

# ML
scikit-learn>=1.4.0

# Airflow (Note: Airflow 2.7 may still complain on 3.14; upgrade if it fails)
apache-airflow==2.7.3

# Monitoring
langfuse==2.7.3

# Groq
groq==0.4.1

# Utilities
python-dotenv==1.0.0
rich>=13.6.0
tabulate==0.9.0
